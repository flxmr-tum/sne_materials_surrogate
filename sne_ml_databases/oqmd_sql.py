#1) download & unzip the database from oqmd.org
#2) setup a container (simplest with bubblewrap.py). make sure that a folder /run/mysqld is there (and accessible from outside!)!
#  - make all the necessary dirs for mariadb until it starts :D
#  - and use the script `mariadb-install-db --user=root --basedir=/usr --ldata=/var/lib/mysql to create all the stuff.
